name: My First workflows
on:[push]
jobs:
    First_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "Hello,We are learing Github Actions"

            - name: List all the files
              run:ls

            - name: Read the file
              run: cat README.md 